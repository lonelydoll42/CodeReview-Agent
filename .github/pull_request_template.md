## 变更目的

说明触发场景、修改前后的行为，以及涉及的模块。

## 验证

- [ ] `python -m pytest -q`
- [ ] `ruff check .`
- [ ] 新配置已同步到 `.env.example`，未提交本地环境与密钥
- [ ] 审查相关修改已覆盖 SHA、diff 行号或缓存失效场景

## 风险与回退

说明兼容性、数据迁移（如有）及需要回退的提交。
