[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "strict"

[tool.ruff]
target-version = "py310"
exclude = [".venv"]

[tool.ruff.lint]
# Enforce syntax and undefined-name errors without a repository-wide style rewrite.
select = ["E9", "F63", "F7", "F82"]
